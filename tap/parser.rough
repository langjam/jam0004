tokens = [...]
n = 0


while n < len(tokens):
	if tokens[n].type == KEYWORD:
		n += 1
		if tokens[n].type == KEYWORD:
			match f"{tokens[n-1].value}{tokens[n].value}":



when keyword is detected:
	make sure that keyword is in ("thumb", "index", "middle", "ring", "pinky")
	if not -> raise Error

	make sure that next token is a keyword in ("soft", "middle", "hard")
	if not -> raise Error

	
