oscillator sine(): sample {
    return sin(phase())
}

sound main() {
    for p in [440, 460, 480] {
        do t = 0 ; t < 1 ; t + SAMPLE_PERIOD {
            let s = 0.3 * sine() at time_phase(t, p)
            mix(pan(s, sin(t * TAU)))
            next()
        }
    }
}
