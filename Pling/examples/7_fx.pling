// add effect to audio source

use audio;
use math;

#audio.begin;

#printnf `Audio initialized: `;
#print #audio.initted;

var handle = #audio.new;
var source = #audio.microphone.new;

#audio.bindsource handle, source;
#audio.efx.pitch handle, 1.2;
//#audio.efx.reverb handle;
#audio.start handle;

